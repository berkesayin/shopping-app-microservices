spring:
  application:
    name: search-service
  elasticsearch:
    uris: https://localhost:9200
    username: elastic
    password: 5Ddtx05yH9wsv2MofHuZ
    ssl:
      ca-certificate: /Users/berkesayin/Documents/apps/elasticsearch-8.17.4/config/certs/http_ca.crt
      verification-mode: certificate
    connection-timeout: 5s
    socket-timeout: 3s
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: search-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: "dev.berke.app.consumer.event.*"
            type:
              mapping: >
                productPublished:dev.berke.app.consumer.event.ProductPublishedEvent,
                productUnpublished:dev.berke.app.consumer.event.ProductUnpublishedEvent,
                orderCreated:dev.berke.app.consumer.event.OrderCreatedEvent
app:
  kafka:
    topics:
      product-events: product-events
      order-created: order-creations
server:
  port: 8015

