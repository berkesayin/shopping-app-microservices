name: Customer Build

on:
  push:
    branches:
      - master
    paths:
      - 'services/customer/**'
  pull_request:
    branches:
      - master
    paths:
      - 'services/customer/**'

jobs:
  customer-build:
    uses: ./.github/workflows/app-base-build.yml
    with:
      service-name: customer
      java-version: 21